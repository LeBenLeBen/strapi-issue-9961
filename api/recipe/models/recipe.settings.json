{
  "kind": "collectionType",
  "collectionName": "recipes",
  "info": {
    "name": "Recipes",
    "description": ""
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "attributes": {
    "title": {
      "type": "string",
      "required": true
    },
    "author": {
      "via": "recipes",
      "plugin": "users-permissions",
      "model": "user"
    }
  }
}
